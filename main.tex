\documentclass[sigplan,anonymous,review]{acmart}

\settopmatter{printacmref=false}
\renewcommand\footnotetextcopyrightpermission[1]{}
\setcopyright{none}

\usepackage{amsmath,amsfonts}
\usepackage{algorithm}
\usepackage{algorithmic}
\renewcommand{\algorithmiccomment}[1]{\(\triangleright\)\ \textit{#1}}
\usepackage{array}
\usepackage{graphicx}
\usepackage{multirow}
\usepackage{xspace}

\newcommand{\sysname}{\mbox{WISEConv}\xspace}

\begin{document}

\title{\sysname: A Worklist-Driven Masked-Convolution Runtime for GPUs}

\author{Anonymous Authors}

\begin{abstract}
% ORIG (RAL/robotics framing):
% Onboard robotic perception must sustain high update rates under tight compute
% and power budgets. Masking policies from event increments, temporal frame
% differences, and learned gates expose where computation is unnecessary, yet
% existing masked-convolution paths do not convert this sparsity into proportional
% latency reduction.
Masked convolution promises to cut the per-frame inference latency of vision models by
computing only positions an upstream policy marks active, yet existing paths
sacrifice either throughput or useful-compute ratio and fail to convert sparsity
into proportional latency reduction. We present \sysname, a masked-convolution
runtime that replaces the dense tiled pipeline's contiguous coordinate stream
with an active-output worklist derived from the upstream mask; it restricts
computation to active positions while retaining dense tensor layouts
and the regular per-position reduction. Securing both a high useful-compute
ratio and high throughput requires amortizing
construction overhead and sustaining compute throughput as per-frame worklist
lengths vary. \sysname therefore co-designs construction and consumption:
compatible operators reuse the
constructed worklist without reconstruction, the worklist emission order
preserves tile-local spatial reuse, and a data-driven adaptive decomposition
adjusts parallel work to the activity level without host synchronization. Across
four perception models on six GPU platforms (four discrete, two embedded), \sysname
reduces full-model latency by 22.1--46.5\% against the fastest evaluated competing path
and per-frame energy by 28.5--72.6\% relative to dense execution on the
embedded GPUs.
\end{abstract}

\keywords{Sparse convolution, dynamic spatial sparsity, GPU runtime, deep learning
systems, efficient inference}

\maketitle

\input{src/intro}
\input{src/background}
\input{src/overview}
\input{src/design_new}
\input{src/impl}

% ---------------------------------------------------------------------------
% Remaining sections (to be written):
% \input{src/related}
\input{src/eval}
\input{src/conclusion}
% ---------------------------------------------------------------------------

\begin{acks}
% TODO
\end{acks}

\bibliographystyle{ACM-Reference-Format}
\bibliography{refs}

\end{document}
