\section{\sysname Design}
\label{sec:design}

\subsection{Overview}
\label{sec:design:overview}

\sysname resolves the useful-compute ratio vs.\ throughput tension
(Section~\ref{sec:bg:gap}) through a two-stage design
(Figure~\ref{fig:overview}):

\begin{enumerate}
  \item \textbf{Construction stage.} A lightweight kernel scans the input
        active mask $M_{in}$ and evaluates Eq.~\eqref{eq:mask} for each
        output position, emitting an ordered \emph{worklist}
        $\mathbf{q} = (q_1, \ldots, q_m)$ of $m = \|M\|_0$ active output
        indices drawn from $G_{out}$. The worklist preserves tile-local
        spatial locality (Section~\ref{sec:design:construct}).

  \item \textbf{Compute stage.} A convolution kernel consumes $\mathbf{q}$
        as its coordinate source. For each $q_j$, the kernel decodes the
        spatial coordinate, reads the receptive-field input
        $\mathbf{x}[\mathcal{N}(q_j)]$, and accumulates
        Eq.~\eqref{eq:conv} identically to the dense pipeline
        (Section~\ref{sec:design:compute}).
\end{enumerate}

The computed set $\mathcal{C} = \{q_1, \ldots, q_m\}$ contains only active
positions (high useful-compute ratio), while the per-position datapath
remains that of the dense pipeline (near-dense throughput). The
construction stage operates entirely in mask space and its cost is
independent of channel dimensions, making its overhead negligible relative
to the compute stage.

% TODO: overview figure (double column)
\begin{figure*}[t]
  \centering
  \fbox{\parbox{0.9\textwidth}{\centering\vspace{2.5cm}%
    \textbf{[TODO]} \sysname overview. Left: input mask $M_{in}$ on the
    spatial grid. Middle: construction stage scans tiles, emits ordered
    worklist $\mathbf{q}$. Right: compute stage consumes $\mathbf{q}$, drives
    implicit GEMM, writes output $\mathbf{y}$ at active positions
    only.\vspace{2.5cm}}}
  \caption{\sysname two-stage pipeline. The construction stage scans the
  mask tile by tile and emits an ordered worklist. The compute stage
  drives the dense convolution datapath using only the worklist positions.}
  \label{fig:overview}
\end{figure*}


\subsection{Worklist Construction}
\label{sec:design:construct}

The construction stage produces the worklist $\mathbf{q}$ from the input
mask $M_{in}$ by performing a per-tile gather followed by concatenation.
It operates on the tile partition $T_1, \ldots, T_N$ defined in
Eq.~\eqref{eq:tile}. For each output position, the stage first evaluates
Eq.~\eqref{eq:mask} to determine the output mask $M$ (requiring $k^2$
boolean reads of $M_{in}$ per position), then gathers the active indices
tile by tile.

\paragraph{Per-tile gather.}
For each tile $T_n$, the output mask $M$ induces a support set of
active indices within the tile:
\begin{equation}
  \mathcal{I}_n = \bigl\{\, k \in \{1, \ldots, t_h t_w\} : M[T_n[k]] = 1 \,\bigr\}.
  \label{eq:support}
\end{equation}
The per-tile subsequence is the gather of $T_n$ by its support set:
\begin{equation}
  \mathbf{q}_n = (T_n)_{\mathcal{I}_n}
               = (T_n[k])_{k \in \mathcal{I}_n},
  \label{eq:pertile-gather}
\end{equation}
ordered by ascending $k$ (i.e., row-major within the tile), with length
$|\mathbf{q}_n| = |\mathcal{I}_n|$.

\paragraph{Worklist concatenation.}
Let $P = (P_1, \ldots, P_N)$ be the permutation of $\{1, \ldots, N\}$
determined by nondeterministic parallel block scheduling. The worklist is:
\begin{equation}
  \mathbf{q} = (\mathbf{q}_{P_1},\; \mathbf{q}_{P_2},\; \ldots,\;
               \mathbf{q}_{P_N}),
  \label{eq:worklist}
\end{equation}
with total length $|\mathbf{q}| = \|M\|_0$. Each $\mathbf{q}_{P_n}$
retains its internal row-major ordering; only the inter-tile order varies
with $P$.


\paragraph{Cost analysis.}
The construction stage evaluates Eq.~\eqref{eq:mask} for all
$H_{out} W_{out}$ output positions, performing $k^2$ boolean reads of
$M_{in}$ per position, plus $\|M\|_0$ integer writes to form
$\mathbf{q}$. Its total work per layer is
$O(H_{out} W_{out} \cdot k^2)$, independent of $C_{in}$ and $C_{out}$.

To estimate the overhead relative to computation, we model per-layer
latency as $\hat{t} = \hat{t}_{\text{construct}} +
\hat{t}_{\text{compute}}$, assuming each is proportional to its
operation count. Construction performs $H_{out} W_{out} \cdot k^2$
operations; computation performs
$\rho^l \cdot H_{out} W_{out} \cdot k^2 \cdot C_{in} \cdot C_{out}$
FMAs. Under this model the overhead ratio is:
\begin{equation}
  \hat{t}_{\text{construct}} \,/\, \hat{t}_{\text{compute}}
  \;\leq\; 1\,/\,(\rho^l \cdot C_{in} \cdot C_{out}).
  \label{eq:overhead}
\end{equation}
For a layer with $C_{in} = C_{out} = 64$ and $\rho^l = 5\%$, this gives
${\approx}\,0.5\%$; at $\rho^l = 1\%$ it is ${\approx}\,2.4\%$.
Section~\ref{sec:eval} confirms that the measured construction overhead
is consistent with this bound.

\begin{algorithm}[t]
\caption{Worklist Construction}
\label{alg:construct}
\begin{algorithmic}[1]
\REQUIRE Tiles $T_1, \ldots, T_N$, input mask $M_{in}$
\ENSURE Worklist $\mathbf{q}$, output mask $M$
\FOR{each tile $T_n$ \textbf{in parallel}}
  \FOR{each $k \in \{1, \ldots, t_h t_w\}$}
    \STATE $g \leftarrow T_n[k]$
    \STATE $M[g] \leftarrow$ Eq.~\eqref{eq:mask}
  \ENDFOR
  \STATE $\mathcal{I}_n \leftarrow \{k : M[T_n[k]] = 1\}$
  \STATE $\mathbf{q}_n \leftarrow (T_n[k])_{k \in \mathcal{I}_n}$
\ENDFOR
\STATE $\mathbf{q} \leftarrow (\mathbf{q}_{P_1}, \ldots, \mathbf{q}_{P_N})$
  \hfill \COMMENT{$P$ from block scheduling}
\end{algorithmic}
\end{algorithm}


\subsection{Worklist-driven Convolution}
\label{sec:design:compute}

The compute stage fuses position-level selectivity into the dense
pipeline's coordinate-driven execution: it computes Eq.~\eqref{eq:conv}
for exactly the $\|M\|_0$ worklist entries while preserving the
per-position datapath unchanged, summarized as
Algorithm~\ref{alg:compute}.
Following standard implicit GEMM, the workload is partitioned into compute
tiles: the worklist positions are divided into segments of
size $B_M$ indexed by $b_m$, and the $C_{out}$ output channels into
segments of size $B_N$ indexed by $b_n$; all compute tiles execute in
parallel (line~1). Each compute tile first
loads its $B_M$ worklist entries into $\mathbf{q}'$ (line~2); this is the
sole deviation from the dense pipeline, which uses implicitly sequential
coordinates. After initializing the accumulator (line~3), the tile
iterates over the $k^2 C_{in}$ reduction dimension in steps of $B_K$
(lines~4--7): at each step $b_k$, the kernel uses coordinates in
$\mathbf{q}'$ to gather the corresponding input slice from $\mathbf{x}$,
loads the matching weight block, and accumulates the partial product.
Finally, the tile writes the accumulated result back to $\mathbf{y}$ using
the linear indices in $\mathbf{q}'$ as output addresses (line~8).

\paragraph{Locality.}
Each compute tile processes $B_M$ consecutive worklist entries. Because
construction compacts active positions per tile, consecutive entries in
each $\mathbf{q}_{P_n}$ are row-major neighbors in $G_{out}$ whose
receptive fields overlap in contiguous memory. A compute chunk of $B_M$
entries spans at most $\lceil B_M / \min_n |\mathbf{q}_{P_n}| \rceil$
construction tiles regardless of the permutation $P$; the
nondeterministic inter-tile ordering does not degrade spatial locality
relative to the dense raster order.

\begin{algorithm}[t]
\caption{Worklist-driven Convolution}
\label{alg:compute}
\begin{algorithmic}[1]
\REQUIRE Worklist $\mathbf{q}$, input $\mathbf{x}$, weight $\hat{\mathbf{W}}$
\ENSURE Output $\mathbf{y}$ at positions in $\mathbf{q}$
\FOR{each compute tile $(b_m, b_n)$ \textbf{in parallel}}
  \STATE $\mathbf{q}' \leftarrow$ load $B_M$ entries from $\mathbf{q}$ for segment $b_m$
  \STATE Initialize accumulator $\mathbf{Y} \leftarrow \mathbf{0}_{B_M \times B_N}$
  \FOR{$b_k = 1, \ldots, \lceil k^2 C_{in} / B_K \rceil$}
    \STATE $\mathbf{X} \leftarrow$ gather input block from $\mathbf{x}$ at positions $\mathcal{N}(\mathbf{q}')$, reduction step $b_k$
    \STATE $\hat{\mathbf{W}}' \leftarrow$ load weight block for step $b_k$, channels $b_n$
    \STATE $\mathbf{Y} \leftarrow \mathbf{Y} + \mathbf{X} \cdot \hat{\mathbf{W}}'$
  \ENDFOR
  \STATE Write $\mathbf{Y}$ to $\mathbf{y}$ at addresses $\mathbf{q}'$
\ENDFOR
\end{algorithmic}
\end{algorithm}
