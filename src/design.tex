\section{\sysname Design}
\label{sec:design}

The design follows the worklist from its construction
(\S\ref{sec:design:construct}) and propagation across compatible operators
(\S\ref{sec:design:reuse}) to its consumption by the convolution pipeline
(\S\ref{sec:design:compute}).

%% ----------------------------------------------------------------
\subsection{Tile-Local Worklist Construction}
\label{sec:design:construct}
\label{sec:design:construct:compact}

Constructing a tight schedule already requires a scan of the output grid, but
the resulting coordinates must also retain enough spatial structure for
efficient consumption. Globally stable compaction preserves the complete dense
traversal order but requires a prefix scan or another pass to obtain
device-wide offsets; per-coordinate appends avoid that coordination but scatter
neighboring positions. Materializing active receptive fields, as gather-scatter
does, regularizes the compute input at the cost of feature traffic proportional
to the active count and $k^2C_{in}$. The useful middle ground is therefore a
metadata-only schedule with local rather than global ordering.

\sysname realizes this middle ground with contiguous, tile-local worklist
segments. Because the number of active positions is not known before the pass,
it preallocates a coordinate buffer with capacity
$|\mathcal{G}|=H_{out}W_{out}$, the static upper bound on
$|\mathcal{W}|$. Algorithm~\ref{alg:construct} details the resulting single
pass. Line~1 initializes the device-resident length counter. Lines~2--7
partition the output grid into $B_\tau\times B_\tau$ construction tiles, derive
the corresponding entries of $M_{out}$ using Eq.~\ref{eq:mask}, and compact
each tile's active coordinates into a local sequence $\mathcal{P}_\tau$ in
dense-traversal order. When a learned gate supplies $M_{out}$
directly~\cite{dynconv,dgnet}, Lines~4--5 are omitted and Line~7 compacts the
supplied mask. Lines~8--11 count the compacted coordinates, atomically reserve
$[\mathrm{base},\mathrm{base}+n_\tau)$ from the counter, and copy
$\mathcal{P}_\tau$ into that interval. After all tiles complete, the reserved
intervals collectively store $\mathcal{W}$, and the counter records
$n_{\mathcal{W}}$. Because the layer shape fixes the buffer capacity and launch
geometry while only the device observes the data-dependent length, construction
requires no runtime allocation or host-visible active count.

To relate the worklist to the useful-compute ratio of
Eq.~\ref{eq:efficiency-framework}, we insert its length $n_{\mathcal{W}}$ between the
required-position count and the issued-slot count. For
$n_{\mathcal{W}}>0$,
\begin{equation}
  \eta
  = \underbrace{\frac{\|M_{out}\|_0}{n_{\mathcal{W}}}}_{\eta_{\mathrm{construct}}}
  \cdot
  \underbrace{\frac{n_{\mathcal{W}}}{|\mathcal{C}|}}_{\eta_{\mathrm{compute}}} .
  \label{eq:eta-decomp}
\end{equation}
Here, $\eta_{\mathrm{construct}}$ compares required positions with worklist
entries, while $\eta_{\mathrm{compute}}$ compares worklist entries with issued
slots. We analyze the former here and the latter after defining the compute
organization in \S\ref{sec:design:compute}. The factorization decomposes
$\eta$ only; all stage costs remain reflected in throughput~$T$.

Because construction tiles partition the output grid and use disjoint
reservations, each active coordinate appears exactly once, so
$n_{\mathcal{W}}=\|M_{out}\|_0$. Fresh construction therefore has
$\eta_{\mathrm{construct}}=1$ for a nonempty worklist. Coverage constrains
membership rather than order, so tile reservation order does not affect
correctness; traversal order within each interval is retained for locality.

\begin{algorithm}[t]
\caption{Tile-Local Worklist Construction}
\label{alg:construct}
\begin{algorithmic}[1]
\REQUIRE Input mask $M_{in}$, output grid $\mathcal{G}$, tile size $B_\tau$
\ENSURE Mask $M_{out}$, worklist $\mathcal{W}$, length
        $n_{\mathcal{W}}=|\mathcal{W}|$
\STATE $n_{\mathcal{W}} \leftarrow 0$
\FOR{each $B_\tau \times B_\tau$ spatial tile $\tau$ \textbf{in parallel}}
  \FOR{each position $p \in \tau$ \textbf{in parallel}}
    \STATE $\mathrm{active}(p) \leftarrow \exists\, r \in \mathcal{N}(p): M_{in}[r] = 1$
    \STATE $M_{out}[p] \leftarrow \mathrm{active}(p)$
  \ENDFOR
  \STATE $\mathcal{P}_\tau \leftarrow \textsc{Compact}(\tau,\,M_{out})$
  \STATE $n_\tau \leftarrow |\mathcal{P}_\tau|$
  \STATE $\mathrm{base} \leftarrow
         \operatorname{AtomicAdd}(n_{\mathcal{W}},n_\tau)$
  \FOR{each $0\le j<n_\tau$ \textbf{in parallel}}
    \STATE $\mathcal{W}[\mathrm{base}+j] \leftarrow \mathcal{P}_\tau[j]$
  \ENDFOR
\ENDFOR
\end{algorithmic}
\end{algorithm}

\sysname thus confines its ordering guarantee to each contiguous segment:
coordinates retain the tile's dense traversal, while independently reserved
segments provide no cross-boundary locality guarantee. A compute
tile of $B_M$ positions (the output-position tile width used by the compute
stage, \S\ref{sec:design:compute}) may therefore span coordinates from multiple construction segments when
segments are shorter than $B_M$; the preserved locality is intra-segment
contiguity, not compute-tile-to-construction-tile alignment. As shown in
\S\ref{sec:eval:ablation-sensitivity}, this tile-local order achieves compute-stage
throughput comparable to global ordering, making the additional ordering pass
an unfavorable end-to-end trade-off.

\subsection{Cross-Operator Worklist Reuse}
\label{sec:design:reuse}

Each construction pass examines all $H_{out}W_{out}$ positions and, when
deriving $M_{out}$ from $M_{in}$, performs $O(k^2)$ mask work per position.
This channel-independent grid scan contrasts with required convolution work,
which scales with the active ratio, spatial extent, and both channel
dimensions. Repeating the scan
across same-grid layers that introduce no new required positions is therefore
redundant. \sysname instead amortizes construction over each compatible layer
sequence by reusing one worklist across its operators.

For layer $l$, let $\mathcal{G}^l$ denote its output grid, $M^l_{out}$ its
output mask, $\mathcal{W}^l$ its worklist, and $n^l_{\mathcal{W}}$ its
device-resident length. \sysname propagates the mask and worklist metadata
alongside the feature tensor. From layer semantics, \sysname statically infers
that consecutive layers are reuse-compatible when
$\mathcal{G}^{l+1}=\mathcal{G}^l$ and
\begin{equation}
  \{p\in\mathcal{G}^{l+1} \mid M^{l+1}_{out}[p]=1\}
  \subseteq
  \{p\in\mathcal{G}^l \mid M^l_{out}[p]=1\} .
  \label{eq:reuse-condition}
\end{equation}
Because $\mathcal{W}^l$ covers $M^l_{out}$,
Eq.~\ref{eq:reuse-condition} guarantees coverage for $M^{l+1}_{out}$.
\sysname therefore forwards $\mathcal{W}^{l+1}=\mathcal{W}^l$ and
$n^{l+1}_{\mathcal{W}}=n^l_{\mathcal{W}}$ without reconstruction and applies
the condition recursively at each subsequent layer.

Equality in Eq.~\ref{eq:reuse-condition} covers same-grid $1\times1$
convolutions, mask-preserving activations, and inference-time normalization,
leaving $\eta_{\mathrm{construct}}$ unchanged. Strict inclusion can result from
mask-narrowing operators in the upstream policy, such as the activation-fused
update truncation that DeltaCNN uses to increase downstream
sparsity~\cite{deltacnn}. The inherited worklist remains a valid superset,
trading a lower $\eta_{\mathrm{construct}}$ for one fewer construction pass
without violating coverage. Because compute gates all output writes by the
exact mask, positions scheduled but no longer active under the current mask
participate in reduction yet produce no store; the superset schedule therefore
cannot alter any inactive position's observable state.

A change in the output grid violates the geometry constraint, while
receptive-field expansion may violate Eq.~\ref{eq:reuse-condition} by
introducing positions outside the upstream mask; either case invokes the
construction pass of \S\ref{sec:design:construct:compact} to emit a new tight
worklist. A lower $\eta_{\mathrm{construct}}$ alone does not trigger
reconstruction because tightening a valid superset would pay another grid scan.
Common receptive-field-expanding layers such as $3\times3$ convolutions still
create rebuild points throughout the network, leaving device-resident worklist
lengths variable across layers and frames. Compute must therefore handle a
dynamic problem size.

%% ================================================================
%% COMPUTE SECTION — TO BE WRITTEN
%% ================================================================
%%
%% Subsection title: On-Device Adaptive Stream-K Execution
%% The kernel reads its own worklist length on-device, looks up a precomputed
%% policy table, and selects its decomposition mode (DP / aligned Split-K /
%% hybrid Stream-K) — no host readback, no prediction.
%% Stream-K: Osama et al., PPoPP 2023; implemented in CUTLASS 3.x.
%%
%% ----------------------------------------------------------------
%% PARAGRAPH PLAN (5 paragraphs + Algorithm + Figure)
%% ----------------------------------------------------------------
%%
%% Para 1: GEMM mapping + static launch property
%%   Intent: establish compute's mathematical object and the static-launch invariant.
%%   - Worklist-driven conv as implicit GEMM: M = n_W positions, N = C_out, K = k^2 C_in
%%   - Gather-at-load from dense feature tensor, no im2col materialization
%%   - Physical kernel configuration (tile shape, pipeline depth, grid size) fixed
%%     per convolution shape at deployment; host launch path is worklist-length-agnostic
%%   - All per-frame adaptation occurs inside the kernel, driven by the
%%     device-resident worklist length written by construction
%%   - Transition: static launch + variable worklist length → scheduling problem
%%
%% Para 2: Variable-length scheduling problem
%%   Intent: setup tension — motivate why adaptation is needed.
%%   - Dense conv: static tile count → offline-optimal decomposition (DP or fixed Split-K)
%%   - Worklist conv: M-axis tile count proportional to per-frame activity, varies 10-100x
%%   - Two symmetric failure regimes:
%%     (a) High activity, many tiles → K-parallelism (Split-K) is pure overhead
%%     (b) Low activity, few tiles → data-parallel leaves SMs idle (wave quantization)
%%   - Need: per-frame scheduling policy, decided on-device (worklist length is a
%%     device scalar; readback to host would reintroduce synchronization)
%%   - This paragraph states the problem only; the solution follows.
%%
%% Para 3: Stream-K context (3-4 sentences)
%%   Intent: give reviewers enough vocabulary for the three modes. Cite prior work.
%%   NOT a full tutorial — just DP / Split-K / hybrid Stream-K and their tradeoffs.
%%   - Dense GEMM scheduling evolution: DP → Split-K → Stream-K
%%   - Hybrid variant (cite Thakkar et al. / CUTLASS): DP prefix for full waves,
%%     Stream-K tail for the partial wave → eliminates wave quantization, limits
%%     reduction overhead to one wave of CTAs
%%   - Transition: "These strategies assume a tile count known at kernel launch.
%%     When tile count is determined on-device by a preceding construction stage,
%%     the scheduling decision itself must move onto the device."
%%
%% Para 4: Device-resident mode selection (core contribution paragraph)
%%   Intent: the mechanism. Points to Algorithm 2 for formal description.
%%
%%   Offline calibration (two orthogonal argmins, pruning perspective):
%%
%%     Starting assumption: every module runs the full fused three-mode kernel.
%%
%%     Step 1 — Envelope selection (per shape, once):
%%       e*(sigma, d) = argmin_{e in E} T(sigma, d, e, n_full, DP)
%%       - E = physical envelope catalog (tile shape, pipeline depth, occupancy); |E| ~ 4-6
%%       - Selected at full occupancy: compute-bound regime where per-tile throughput
%%         difference is largest. Low-activity bottleneck (parallelism) handled by
%%         mode selection, orthogonal to envelope.
%%
%%     Step 2 — Policy table construction (per tile count, within selected e*):
%%       P[t] = argmin_{(m,s) in M} T(sigma, d, e*, t * B_M, m, s)
%%       - M = {(DP,1)} ∪ {(Split-K, s) : s in {2,4,8,...}} ∪ {(Stream-K)}; |M| ~ 6-8
%%       - Indexed by tile count t = ceil(n_W / B_M); profiled with synthetic worklists
%%       - Two argmins search different spaces, both offline, both over measured latency
%%
%%     Step 3 — Dead-mode elimination (pruning):
%%       Inspect P[t] for all t. If every entry maps to DP, the partial-K scheduling
%%       paths (mode branching, policy table lookup, partial workspace, last-arriver
%%       fixup) are pruned from the fused kernel, producing a dedicated DP kernel.
%%       This is deployment-time dead-path elimination, not a separate design branch.
%%       → Design section only describes the fused kernel.
%%       → Implementation mentions: "shapes whose policy table contains only DP entries
%%          are specialized into a dedicated kernel that elides mode branching and
%%          partial reduction."
%%       → YOLOv8n: 31/60 dedicated (1x1 and shallow 3x3, K_iters ≤ 9),
%%                  29/60 fused (deep 3x3, K_iters ≥ 18).
%%
%%   Runtime mechanism:
%%   - Construction writes n_W as device scalar
%%   - Compute kernel reads n_W at entry → computes tile count → looks up P[t]
%%   - Policy table maps tile count to three regimes:
%%     * Data-parallel: tile count >= several waves → each tile owns full K reduction
%%     * Aligned Split-K: tile count < one wave, K deep enough → uniform K-partition
%%     * Hybrid Stream-K: DP prefix for full waves, Stream-K tail for partial wave
%%   - Three modes share one kernel binary and reduction datapath; only
%%     CTA-to-iteration-range mapping differs
%%
%%   Dead-mode elimination (one sentence, detail in Implementation):
%%   - If P[t] maps to DP for all t, the partial-K scheduling paths are pruned,
%%     producing a dedicated kernel that elides mode branching and partial reduction.
%%
%% Para 5: Last-arriver partial reduction (2-3 sentences)
%%   Intent: close the loop on how Split-K/Stream-K partials are resolved.
%%   - Multiple CTAs sharing a tile's K range → each writes partial to workspace
%%   - Per-tile completion tracking: last arriver reduces + writes output
%%   - No global barrier
%%
%% ----------------------------------------------------------------
%% ALGORITHM 2: Device-Resident Worklist Computation
%% ----------------------------------------------------------------
%% Shows the kernel's runtime decision flow, NOT the GEMM mainloop.
%% Highlights vs vanilla Stream-K:
%%   1. Lines 1-2: device-side runtime scheduling (read n_W from device, lookup P[t])
%%   2. Lines 4-20: three-mode branching in one kernel (DP / Split-K / Stream-K)
%%   3. Line 14: G = persistent grid size (occupancy-limited), not host-passed
%%   4. Lines 21-23: last-arriver fixup without grid sync
%%
%% ----------------------------------------------------------------
%% FIGURE: Three-mode decomposition diagram
%% ----------------------------------------------------------------
%% Placed near Para 4. Three columns showing the same worklist (e.g. 5 tiles x
%% 8 K-iters) decomposed under DP / aligned Split-K / hybrid Stream-K.
%% Axes: CTA (vertical) x iteration range (horizontal), color = output tile.
%% Visual, not pseudocode — lets Para 4 skip iteration-space details.
%%
%% ================================================================

%% ----------------------------------------------------------------
\subsection{On-Device Adaptive Stream-K Execution}
\label{sec:design:compute}


\begin{algorithm}[htb]
\caption{On-Device Adaptive Hybrid Stream-$K$}
\label{alg:streamk}
\begin{algorithmic}[1]
\REQUIRE $\mathcal{W}$, $P$, $g_{\mathrm{res}}$,
         $B_M$, $B_N$, $B_K$, $\mathbf{x}$, $\mathbf{f}$, $k$, $\mathbf{Z}$
\ENSURE $\mathbf{y}$
\STATE $n_{\mathcal{W}} \leftarrow$ \textbf{read} device-resident worklist length
\STATE $s_M \leftarrow \lceil n_{\mathcal{W}} / B_M \rceil$;\;
       $s_N \leftarrow \lceil C_{out} / B_N \rceil$;\;
       $s \leftarrow s_M \times s_N$
\STATE $S_k \leftarrow P[s]$ \COMMENT{policy lookup}
\STATE $K_{\mathrm{iters}} \leftarrow k^2 \lceil C_{in} / B_K \rceil$
\STATE \COMMENT{---------------------------Data-parallel prefix---------------------------}
\STATE $s_{\mathrm{dp}} \leftarrow s - (s \bmod g_{\mathrm{res}})$ \COMMENT{full-wave boundary}
\FOR{$b = \mathrm{bid},\; \mathrm{bid}{+}g_{\mathrm{res}},\; \ldots < s_{\mathrm{dp}}$}
  \STATE $(b_m, b_n) \leftarrow \textsc{Decode}(b,\, s_N)$
  \STATE $\mathbf{w} \leftarrow \mathcal{W}[b_m B_M \,..\, (b_m{+}1) B_M)$ \COMMENT{worklist indirection}
  \STATE $\mathbf{Y} \leftarrow \mathbf{0}_{B_M \times B_N}$
  \FOR{$j = 0, \ldots, K_{\mathrm{iters}}{-}1$}
    \STATE $(i, c_k) \leftarrow \textsc{Offset}(j)$ \COMMENT{kernel offset, channel tile}
    \STATE $\mathbf{X} \leftarrow 
           \mathbf{x}[\mathcal{N}_{i}(\mathbf{w}),\;
           c_k B_K{:}(c_k{+}1) B_K)$
    \STATE $\mathbf{F} \leftarrow
           \mathbf{f}_{i}[c_k B_K{:}(c_k{+}1) B_K,\;b_n B_N{:}(b_n{+}1)B_N)$
    \STATE $\mathbf{Y} \leftarrow \mathbf{Y} + \mathbf{X} \cdot \mathbf{F}$
  \ENDFOR
  \STATE $\mathbf{y}[\mathbf{w},\; b_n B_N{:}(b_n{+}1) B_N) \leftarrow \mathbf{Y}$
\ENDFOR
\STATE \COMMENT{-----------------------------Stream-$K$ tail-----------------------------}
\STATE $s_{\mathrm{tail}} \leftarrow s - s_{\mathrm{dp}}$
\FOR{$b = \mathrm{bid},\; \mathrm{bid}{+}g_{\mathrm{res}},\; \ldots
     < s_{\mathrm{tail}} \times S_k$}
  \STATE $(b_m, b_n, r) \leftarrow \textsc{TailDecode}(b,\, s_N,\, S_k,\, s_{\mathrm{dp}})$
  \STATE $(k_{\mathrm{lo}}, k_{\mathrm{hi}}) \leftarrow
         \textsc{Partition}(K_{\mathrm{iters}},\, S_k,\, r)$ \COMMENT{$r$-th $K$-slice}
  \STATE $\mathbf{w} \leftarrow \mathcal{W}[b_m B_M \,..\, (b_m{+}1) B_M)$
  \STATE $\mathbf{Y} \leftarrow \mathbf{0}_{B_M \times B_N}$
  \FOR{$j = k_{\mathrm{lo}}, \ldots, k_{\mathrm{hi}}{-}1$}
    \STATE $(i, c_k) \leftarrow \textsc{Offset}(j)$
    \STATE $\mathbf{X} \leftarrow 
           \mathbf{x}[\mathcal{N}_{i}(\mathbf{w}),\;
           c_k B_K{:}(c_k{+}1) B_K)$
    \STATE $\mathbf{F} \leftarrow
           \mathbf{f}_{i}[c_k B_K{:}(c_k{+}1) B_K,\;b_n B_N{:}(b_n{+}1)B_N)$
    \STATE $\mathbf{Y} \leftarrow \mathbf{Y} + \mathbf{X} \cdot \mathbf{F}$
  \ENDFOR
  \STATE $\mathbf{Z}[b_m, b_n][r] \leftarrow \mathbf{Y}$ \COMMENT{partial to workspace}
  \IF{\textsc{LastArriver}($b_m, b_n$)}
    \STATE $\mathbf{y}[\mathbf{w},\; b_n B_N{:}(b_n{+}1) B_N) \leftarrow \sum_{r'=0}^{S_k-1} \mathbf{Z}[b_m, b_n][r']$
  \ENDIF
\ENDFOR
\end{algorithmic}
\end{algorithm}

Construction and reuse fix $\eta_{\mathrm{construct}}$; the compute stage
controls $\eta_{\mathrm{compute}}$ and throughput~$T$. Dense convolution maps
to an implicit GEMM with $M$ output positions, $N=C_{out}$ output channels, and
reduction depth $K=k^2 C_{in}$. An output tile covers $B_M$ positions and
$B_N$ output channels; the reduction over $K$ proceeds in steps of $B_K$.
In the standard \emph{data-parallel} decomposition, each of the
$\lceil M/B_M\rceil \times \lceil N/B_N\rceil$ output tiles is assigned to one
thread block, which performs the full $K$ reduction; split-$K$ instead
replicates each tile across $S_k$ blocks that each reduce a disjoint
$K$-slice. Because $M$, $N$, and $K$ are static, the tile shape $(B_M, B_N)$,
reduction step $B_K$, and split factor can be selected once offline by
profiling and keeping the fastest.

In worklist convolution, $N$ and $K$ remain static, but the
$M$~dimension becomes $n_{\mathcal{W}}$, the device-resident worklist length
written by construction, which varies by an order of magnitude or more across
frames of a single layer. The offline assumption breaks: a single configuration
cannot serve the full range. Two failure regimes bracket the problem. When
activity is high and output tiles are plentiful, $K$-axis parallelism adds
coordination and accumulation overhead without improving occupancy. When
activity is low and tiles are scarce, data-parallel execution leaves
streaming multiprocessors (SMs) without work because the tile count no longer
fills a full \emph{wave} (the set of thread blocks that co-reside across all
SMs), referred to as the problem of wave quantization.
Adapting between these regimes requires knowing $n_{\mathcal{W}}$; transferring it to the host inserts a
per-operator synchronization on the critical path.

\sysname builds on Stream-$K$~\cite{streamk}, the established solution to wave
quantization in dense GEMM. Stream-$K$ launches a \emph{persistent grid} of blocks equal
to the device's SM occupancy (not the tile count) and partitions all tiles'
$K$-iterations into $S_k$ units consumed by grid stride; partial $K$-slices
fill SMs that would otherwise sit idle after the data-parallel waves complete.
Its hybrid variant~\cite{cutlass} restricts the $K$-splitting to the partial
tail wave, handling complete waves data-parallel to avoid partial-reduction
overhead where it is unnecessary. In worklist convolution the tile count
changes every frame, so the wave boundary shifts with it. This shifting
boundary is the operating point of \sysname's \emph{on-device adaptive
Stream-$K$ execution}: the compute kernel reads the exact worklist length on
the device, determines the current wave boundary, and selects between
data-parallel, Stream-$K$, and hybrid Stream-$K$ decomposition accordingly,
without host involvement.

Because the host cannot observe $n^l_{\mathcal{W}}$ without synchronization,
\sysname fixes the tile configuration~$\hat{q}^l=(B_M,B_N,B_K)$ per layer at
deployment and adapts only the decomposition mode on-device. What changes per
frame is how the $K$~reduction across output tiles is assigned to thread
blocks, selected from three modes:
\begin{itemize}
  \item \emph{Data-parallel}: each block owns a complete output tile and
    reduces the full $K$~range.
  \item \emph{Stream-$K$}: each output tile's $K$-iterations are
    partitioned into $S_k$ units; the persistent grid consumes all
    units by grid stride, and partial results from blocks sharing
    a tile are resolved through the workspace.
  \item \emph{Hybrid Stream-$K$}: a data-parallel prefix handles complete
    waves; a streaming tail distributes the remaining tiles' $K$-iterations
    across one wave of blocks, eliminating partial-wave waste without applying
    the streaming overhead to the entire grid.
\end{itemize}

Both $\hat{q}^l$ and the per-length mode choice are calibrated offline.
\sysname selects~$\hat{q}^l$ from a small catalog~$\mathcal{Q}$ by profiling
each configuration at the full worklist $\mathcal{W}^l_{\mathrm{full}}$ (all
$|\mathcal{G}^l|$ output positions) and keeping the fastest. Let
$t(q,\mathcal{W})$ denote the minimum kernel latency of configuration~$q$ on
worklist~$\mathcal{W}$ over all mode candidates:
\begin{equation}
  \hat{q}^l
  = \arg\min_{q\in\mathcal{Q}}\;
    t(q,\;\mathcal{W}^l_{\mathrm{full}}).
  \label{eq:tileconfig}
\end{equation}
At the full worklist $\eta_{\mathrm{compute}}\approx 1$, so this step
essentially maximizes throughput~$T$, ensuring competitiveness with dense
execution. The choice of $B_M$ also determines $\eta_{\mathrm{compute}}$ at
every other worklist length: the issued slot count is
$|\mathcal{C}|=B_M\lceil n^l_{\mathcal{W}}/B_M\rceil$, so
$\eta_{\mathrm{compute}}=n^l_{\mathcal{W}}/|\mathcal{C}|$ can drop as
$n^l_{\mathcal{W}}$ shrinks relative to~$B_M$. Fixing $B_M$ per layer is a
necessary constraint, but per-shape profiling partially mitigates the cost:
a large~$B_M$ on a small output grid often produces too few tiles to fill the device
even at full worklist, so such layers naturally select a smaller~$B_M$ that
also limits $\eta_{\mathrm{compute}}$ loss at low activity. The mode table below
compensates for the remaining throughput gap by adapting the $K$-splitting
strategy on-device.

Within~$\hat{q}^l$, \sysname builds a \emph{policy table}~$P^l$ indexed by
tile count $s=\lceil n^l_{\mathcal{W}}/B_M\rceil \times \lceil C_{out}/B_N\rceil$. For each~$s$ it profiles
every candidate mode~$m$ and split factor~$S_k$:
\begin{equation}
  P^l[s]
  = \arg\min_{m,\,S_k}\;
    t(\hat{q}^l,\;s,\;m,\;S_k),
  \label{eq:policy}
\end{equation}
where the minimization ranges over the three modes and their compatible split
factors. All three modes process the same output tiles and differ only in how
$K$-iterations are assigned to blocks, so $\eta_{\mathrm{compute}}$ is
invariant across modes and minimizing latency directly maximizes
throughput~$T$, and hence $\eta_{\mathrm{compute}}\,T$.

Algorithm~\ref{alg:streamk} presents the hybrid mode, which subsumes the
other two: data-parallel execution corresponds to $s_{\mathrm{dp}}=s$ (empty
tail), while pure Stream-$K$ corresponds to $s_{\mathrm{dp}}=0$ (no
data-parallel prefix); the policy table selects among the three based on the
current tile count. All three modes share the same gather-at-load reduction
datapath; only the block-to-work mapping differs.

At runtime, the kernel reads $n_{\mathcal{W}}$, derives
the tile count, and looks up the decomposition mode from the policy table $P^l$
(Lines~1--4 in Algorithm~\ref{alg:streamk}).
The persistent grid first processes complete waves data-parallel
(Lines~7--18): each block decodes its position and channel tile indices,
reads $B_M$ output coordinates from the worklist $\mathcal{W}$, and reduces the full
reduction depth, writing the result directly to the output.
The remaining partial-wave tiles enter the Stream-$K$ tail
(Lines~21--36): the reduction depth of each tile is partitioned among $S_k$
contributors (the split factor selected by the policy table), and each block
reduces its assigned slice into a pre-allocated workspace slot for that tile.
Each slot holds a $B_M \times B_N$ partial accumulator in accumulation
precision (typically FP32); the workspace $\mathbf{Z}$ is sized to
$\max(S_k) \times |\mathbf{y}^l|$ elements per layer, where $\max(S_k)$
is the largest split factor in the policy table $P^l$. A per-tile
completion counter tracks how many contributors have finished; the last block
to arrive loads all partial fragments, reduces them, applies the output
epilogue, and writes the final result (Lines~33--35). No device-wide barrier
is needed; data-parallel tiles bypass the workspace entirely and write output
directly.

