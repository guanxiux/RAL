\section{Background and Motivation}
\label{sec:background}

\subsection{2D Dense Convolution on Embedded Platforms}
\label{sec:bg:dense}

Convolutional neural networks (CNNs) are the computational backbone of onboard
robotic visual perception. CNN-based depth estimation and optical
flow~\cite{fastdepth,raft,nanoflownet} feed obstacle avoidance and path
planning; object detection~\cite{yolov7} and semantic
segmentation~\cite{bisenet} provide scene understanding for navigation; and
pose estimation~\cite{densefusion,contactgraspnet} enables closed-loop
manipulation. While vision transformers~\cite{vit} have achieved strong
accuracy on these tasks, their quadratic attention cost makes them
impractical under the tight latency and power budgets of embedded platforms;
CNN-based models remain the dominant deployed choice for real-time onboard
visual inference. Across these models, convolutional layers operating on
image-grid feature maps dominate the inference workload and sit directly on
the control-critical path.

On embedded platforms (Jetson Xavier NX, Orin, edge TPUs), power and thermal
budgets limit available compute to a fraction of server-class GPUs, yet the
closed control loop imposes a hard latency ceiling: perception must complete
within the sensor frame interval (typically tens of milliseconds) to feed
downstream planning and control in time.

A 2D convolution layer transforms an input feature map
$\mathbf{x} \in \mathbb{R}^{H_{in} \times W_{in} \times C_{in}}$ into an
output $\mathbf{y} \in \mathbb{R}^{H_{out} \times W_{out} \times C_{out}}$
using a weight tensor
$\mathbf{W} \in \mathbb{R}^{C_{out} \times C_{in} \times k \times k}$,
where $k$ is the kernel size and $k \times k$ is the receptive field size.
The computation is defined per position on the output grid
$\mathcal{G}_{out} = \{1,\dots,H_{out}\} \times \{1,\dots,W_{out}\}$. For each
$p \in \mathcal{G}_{out}$, let $\mathcal{N}(p) \subset \mathcal{G}_{in}$ denote
the set of $k^2$ input positions in the receptive field of $p$. The layer
computes:
\begin{equation}
  \mathbf{y}[p] = \sum_{i=1}^{k^2} \mathbf{W}_i \; \mathbf{x}[\mathcal{N}_i(p)],
  \label{eq:conv}
\end{equation}
where $\mathcal{N}_i(p)$ is the $i$-th position in the receptive field and
$\mathbf{W}_i \in \mathbb{R}^{C_{out} \times C_{in}}$ is the corresponding
weight slice. The output at each position is thus an accumulation of
$k^2$ weighted contributions from the input. On the GPU, $\mathcal{G}_{out}$ is
partitioned into $N$ disjoint tiles
$\mathcal{G}_{out} = T_1 \cup \dots \cup T_N$, $T_i \cap T_j = \emptyset$
for $i \neq j$, where each tile $T_j$ is a contiguous spatial block of
output positions. Tiles are processed in parallel; within each tile,
positions are spatially adjacent, so their receptive fields overlap and lie
in contiguous memory. This regularity enables cuDNN-class~\cite{cudnn}
throughput. We denote by $C \subseteq \mathcal{G}_{out}$ the set of
positions actually computed by a given execution strategy. Dense convolution
sets $C = \mathcal{G}_{out}$ unconditionally, which on embedded platforms
often exceeds the latency budget.

\subsection{Masked Convolution for Onboard Perception}
\label{sec:bg:masked}

\subsubsection{Mask Sources}
\label{sec:bg:masks}

Many onboard vision systems exploit the fact that only a subset of the
input changes between inference calls or is relevant to the current
decision. Let
$\mathcal{G}_{in} = \{1,\dots,H_{in}\} \times \{1,\dots,W_{in}\}$ denote
the input spatial grid. These systems identify an input active mask
$M_{in} \subseteq \mathcal{G}_{in}$ marking positions where the input has
changed or is task-relevant. An output position $p$ must be recomputed
whenever its receptive field intersects the input mask, giving the output
active mask:
\begin{equation}
  M = \bigl\{\, p \in \mathcal{G}_{out} \;\big|\; \mathcal{N}(p) \cap M_{in} \neq \emptyset \,\bigr\}.
  \label{eq:mask}
\end{equation}
In a multi-layer network, the output mask of layer $l$ becomes the input
mask of layer $l{+}1$, propagating sparsity through the model. Three
families of mask sources produce the initial $M_{in}$:

% PLACEHOLDER_REST

\paragraph{Event-camera increments.}
Event cameras~\cite{eventsurvey} are bio-inspired sensors in which each
pixel independently fires when its brightness change exceeds a threshold,
generating data only where intensity varies. They offer microsecond temporal
resolution, high dynamic range, and negligible motion blur, making them
widely used on UAVs and high-speed platforms. Events accumulated over a
temporal window are differenced to reveal which positions received new
activity, producing a naturally sparse
mask~\cite{asyncsparse,evconv}.

\paragraph{Temporal frame differences.}
Standard frame-based cameras on mobile robots and autonomous vehicles
capture video at a fixed rate. Between consecutive frames most of the scene
remains static, especially under moderate ego-motion. Temporal differencing
methods~\cite{cbinfer,deltacnn} compare consecutive frames (or
motion-compensated histories) and identify positions where pixel differences
exceed a threshold; only outputs affected by these positions need
recomputation.

\paragraph{Learned spatial gating.}
Rather than relying on temporal change, dynamic networks~\cite{dgnet} train
a lightweight gating module that examines the current input and predicts,
per spatial region, whether computation is needed for the downstream task.
Regions classified as low-value are masked out, adapting the compute budget
to task-relevant content per frame.


\subsubsection{Execution Paradigms}
\label{sec:bg:paradigms}

Given a mask $M$, the system must compute $\mathbf{y}[p]$ only for
$p \in M$. Existing methods follow one of two paradigms:

\paragraph{Tile skipping.}
A tile $T_j$ is executed if and only if $T_j \cap M \neq \emptyset$;
otherwise it is skipped entirely. The computed set is
$C = \bigcup_{j:\, T_j \cap M \neq \emptyset} T_j$. Because non-skipped
tiles run the unmodified dense pipeline, throughput is preserved. However,
$|C|$ can be much larger than $|M|$ when active positions are scattered
across many tiles: a single active position forces its entire tile to
execute. SBNet~\cite{sbnet}, Skip-Convolutions~\cite{skipconv},
Ev-Conv~\cite{evconv}, and DeltaCNN~\cite{deltacnn} follow this paradigm.

\paragraph{Gather-scatter.}
Active positions are extracted exactly: the system gathers
$\{\mathbf{x}[\mathcal{N}(p)] : p \in M\}$ into a contiguous buffer,
computes Eq.~\eqref{eq:conv} for each, and scatters results back to the
output grid. The computed set is $C = M$ exactly, so no computation is
wasted. However, the gather, scatter, and irregular memory access patterns
abandon the dense pipeline's regular execution, reducing throughput to a
fraction of tile-based methods.
DGNet~\cite{dgnet} and SAI~\cite{sai} follow this paradigm.

\subsection{The Gap: Sparsity Does Not Become Speedup}
\label{sec:bg:gap}

For a network with $L$ convolution layers, we define the \emph{active
ratio} $\rho = \frac{1}{L}\sum_{l=1}^{L} |M^l|/|\mathcal{G}_{out}^l|$,
the average fraction of output positions that require computation across
all layers. We characterize masked-convolution efficiency along two axes:
\emph{throughput}, the rate of convolution operations per second (bounded by
the dense pipeline), and \emph{useful-compute ratio}, the fraction of
executed operations that produce needed outputs ($|M|/|C|$). Tile skipping
preserves throughput but sacrifices useful-compute ratio; gather-scatter
achieves high useful-compute ratio but sacrifices throughput. Neither
delivers both, so upstream sparsity does not translate into proportional
latency reduction. Figure~\ref{fig:gap} quantifies this on Jetson Orin
running XX on YY dataset: with $\rho$ in the range XX--YY\%,
tile-skipping methods achieve only AA--BB\% useful-compute ratio and
gather-scatter methods reach only CC\% of tile-skipping throughput, reducing
convolution latency by only DD--EE\% rather than the ideal $\rho$-linear
scaling. This gap motivates \sysname.

% TODO: figure showing active-output ratio vs actual speedup on Jetson
\begin{figure}[t]
  \centering
  \fbox{\parbox{0.9\columnwidth}{\centering\vspace{2cm}%
    \textbf{[TODO]} Active ratio $\rho$ (x-axis) vs.\ actual speedup over
    dense (y-axis) on Jetson Orin. Three mask sources, two paradigms.
    Dashed line = ideal linear scaling.\vspace{2cm}}}
  \caption{Active ratio vs.\ actual convolution speedup on Jetson
  Orin for the three mask sources. The dashed line marks ideal (linear)
  scaling. Tile skipping and gather-scatter both fall far short.}
  \label{fig:gap}
\end{figure}

\subsection{3D Sparse Convolution: A Different Problem}
\label{sec:bg:3d}

3D sparse convolution (SpConv~v2~\cite{spconv2},
TorchSparse++~\cite{torchsparsepp}) targets point-cloud workloads where
input data is inherently irregular and the core challenge is imposing
computational structure on scattered voxels. \sysname faces a fundamentally
different problem: the input resides on a regular dense grid with
structured, stride-based access; the challenge is how to skip inactive
outputs without leaving the dense pipeline's already-efficient execution.
Applying 3D sparse convolution directly to this 2D scenario performs poorly
because it requires building coordinate hash tables and neighbor maps with
mandatory GPU synchronization, and the per-frame mask changes prevent
amortizing this cost across layers. On Jetson Orin, SpConv~v2 applied to a
2D masked convolution with $\rho = \text{XX}\%$ runs XX$\times$ slower than
the dense baseline due to construction overhead alone
(Section~\ref{sec:eval}).
